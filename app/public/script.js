
// Form Validation
// ===================================

    /*function validateForm(){

      var surveyArray = [];
      var surveyScores = $(".sRange");
      for (var i = 0; i < surveyScores.length; i++) {
        surveyArray.push(parseInt(surveyScores[i].options[surveyScores[i].selectedIndex].value))
      }

      var name = $("#sName").val().trim();
      var photo = $("#sPhoto").val().trim();

      if (name === "" || photo === ""){
        alert("Fields can not be empty")
        return false;
      } else {

        var newFriend =  {
              name: name,
              photo: photo,
              scores: surveyArray,
        }
        return newFriend;
      }
    }


  })

})*/
